# Local Flutter SDK checkout (not part of the app; CI uses flutter-action).
/flutter/

.vscode/

# Packaged handoff (do not version).
vavel_monolith.zip
_export_zip_staging/

# Local release keystore (never commit).
vavel-release.jks

# One-off local APK/AAB handoff (large binaries; do not commit).
release_production_artifacts/

# Local secrets
vavel_app/.env
